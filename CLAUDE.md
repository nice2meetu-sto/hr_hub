# hr_hub 작업 가이드 (Claude용)

새 세션에서 이 프로젝트를 이어받으면 **먼저 `docs/SERVER_GUIDE.md`를 읽을 것.**

## 핵심 규칙

- `hr/` = hr.sto.or.kr (직원용), `hr2/` = hr2.sto.or.kr (인사 담당자용). 서버 웹루트와 1:1 대응.
- 파일명은 **고정 간판 이름**(`index.html`, `mileage.html`, `budgetcal.html`, `edu.html`, `hrtoday.html`, `hrhistory.html`)만 사용. 파일명에 날짜/버전을 붙이지 않는다 — 버전 관리는 git.
- **index.html 자리에는 허브(대문) 페이지만 간다.** 다른 페이지를 index로 만드는 실수가 실제로 2회 있었음.
- hr 허브와 hr2 허브는 쌍둥이 구조 — 한쪽을 고치면 다른 쪽도 같은 문제가 없는지 확인.
- 허브 페이지는 JS `TOOLS` 배열에 항목을 추가하면 카드가 자동 생성되는 구조.
- 페이지 내 링크는 `github.io` 절대경로가 아니라 **서버 상대경로**를 쓴다 (GitHub Pages → 자체 서버 이전 중).
- 서버는 HTTPS 미적용 — 안내 문구/링크에 `http://`를 명시.
- `edu.html`(교육 현황)은 **다른 담당자 관리 영역** — 이 레포에 파일이 없는 게 정상이며, 서버에는 별도로 배포되어 있음. 허브 카드의 `status: "ok"`를 "고치려" 하지 말 것.

## 보안

- **이 레포는 public.** 서버 IP, 계정명, 비밀번호, 방화벽(ACG) 상세를 절대 커밋하지 않는다.
- 직원 개인정보(이름, 생년월일 등)가 든 데이터 파일도 커밋 금지.

## 배포

- 이 클라우드 환경에서는 사내 서버에 직접 접속 불가 (ACG로 사내망/VPN만 허용).
- 여기서는 소스 수정·커밋·푸시까지만 하고, 실제 배포는 사용자가 PC에서 수행 (`docs/SERVER_GUIDE.md` 4장 절차).
- 작업 완료 시 사용자에게 "배포가 필요한 파일 목록"을 알려줄 것 (예: `hr2/hrtoday.html` → 서버 `/usr/share/nginx/html/hr2/hrtoday.html`).
